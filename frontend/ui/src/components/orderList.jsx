import React from 'react';
import OrderTable from './OrderTable';  // Make sure StockTable is correctly imported

export const Orderlist = () => {
  return (
    <div>
      <OrderTable />
    </div>
  );
};

export default Orderlist;